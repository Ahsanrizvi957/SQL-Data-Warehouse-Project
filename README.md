# SQL-Data-Warehouse-Project
Overview:
This project demonstrates the development of a data warehouse and ETL pipeline using SQL based on the Medallion Architecture.The project processes 6 raw CSV files from CRM and ERP source systems, loads them into the Bronze layer, transforms and cleans the data in the Silver layer, and prepares the data for analytical use.
